import 'package:flutter/material.dart';  
// Imports Flutter material package (provides UI components and Color class)

class AppColors {  
  // A class to store all app colors in one place (for consistency)

  static const Color primaryRed = Color(0xFFDB3022);  
  // Main theme color (used for buttons, highlights, etc.)

  static const Color background = Color(0xFFF9F9F9);  
  // Background color of screens

  static const Color white = Colors.white;  
  // Standard white color (from Flutter predefined colors)

  static const Color black = Color(0xFF222222);  
  // Dark color for text or elements

  static const Color grey = Color(0xFF9B9B9B);  
  // Grey color (used for hints, secondary text, etc.)

  static const Color success = Color(0xFF2AA952);  
  // Green color (used for success messages or actions)

  static const Color error = Color(0xFFF01F0E);  
  // Red color (used for errors, warnings, validation messages)
}
